spring.application.name=backend
#server.port=8081
spring.profiles.active=${ENV:test}


# JWT signing. JWT_SECRET must be a Base64-encoded key of at least 32 bytes.
# No default is provided on purpose: the application must fail to start rather
# than fall back to a shared or guessable key.
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
